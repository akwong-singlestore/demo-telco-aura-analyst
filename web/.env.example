# Telco Subscriber Experience Command Center
# Environment Configuration

# Polling interval for dashboard updates (milliseconds)
# Default: 5000 (5 seconds)
# Lower values = more real-time, higher database load
# Higher values = less load, slower updates
VITE_POLL_INTERVAL_MS=5000
